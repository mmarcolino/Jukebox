openapi: 3.0.3
info:
  title: Jukebox API
  version: 1.0.0
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: API is up
          content:
            text/plain:
              schema:
                type: string